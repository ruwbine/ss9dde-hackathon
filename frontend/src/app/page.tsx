'use client';

import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";



export default function Home() {
  return (
    <main className="flex min-h-screen items-center justify-center p-4 bg-gray-100">
      {/* <Card className="w-full max-w-md shadow-xl">
        <CardHeader>
          <CardTitle className="text-xl">Hackathon Project</CardTitle>
        </CardHeader>
        <CardContent className="space-y-4">
          <p className="text-gray-700">Нажми на кнопку, чтобы начать.</p>
          <Button onClick={() => alert("Успех!")}>Let's Go!</Button>
        </CardContent>
      </Card> */}
    </main>
  );
}
